#include "main.h"
/**
 * _islower - A funtcion that checks for uppercase character
 * @c: The character to be checked
 * Return: If c is lowercase return 1, otherwise return 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
